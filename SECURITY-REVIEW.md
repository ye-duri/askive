# 보안 점검 결과 — 2026-09-21

## 결론과 범위

기본 보안 조치가 있는 MVP이며, 공개 서비스의 보안 검증이 완료된 상태는 아닙니다. Node 서버, 카메라/합성/인쇄 코드, 문자 발송 UI, 정적 파일, 환경 설정 예제와 ZIP 구성을 점검했습니다. 로컬 모의 서버로 보안 회귀 테스트를 실행했으며 실제 사진·전화번호·SENS 계정이나 외부 서버를 대상으로 공격/발송하지 않았습니다. 클라우드 IAM, TLS, 방화벽, 접근 로그, iPad 관리 상태, 프린터 저장 데이터는 미검증입니다.

## 수정한 항목

| 항목 | 수정 전 위험과 조건 | 변경 |
|---|---|---|
| 정적 파일 노출 | dist 안에 잘못 복사한 비밀 파일이나 외부 경로를 가리키는 심볼릭 링크를 제공할 수 있음. 실제 비밀 파일 노출은 발견하지 않음 | 공개 파일과 frames 이미지 경로 허용 목록, realpath로 배포 폴더 밖 접근 차단 |
| 인증 전 요청 본문 처리 | 비인증 요청도 최대 420KB를 읽은 뒤 인증하여 동시 요청 시 자원 소모 가능 | 인증을 먼저 검사, 로그인 2KB 제한, 본문·헤더 시간/용량 및 연결 수 제한 |
| 잘못된 발송 한도 설정 | MMS_HOURLY_LIMIT=NaN이면 시간당 제한 비교가 무효 | 정수 1~1000 외에는 서버 시작 거부 |
| 입력 형 변환 | 배열 등의 입력이 정규식 검사에서 문자열로 암묵 변환될 수 있음 | JSON 객체 및 전화번호·ID·비밀번호의 명시적 타입 검사, 단순한 Base64 검사 |
| 브라우저 방어 헤더 없음 | 스크립트 삽입이나 iframe 악용에 대한 추가 방어층 부족. 실제 XSS는 발견하지 않음 | CSP, frame-ancestors/X-Frame-Options, nosniff, Referrer/Permissions 정책 |
| 출처/호스트 및 배포 설정 | 요청의 Host를 기반으로 허용 출처를 만들고 외부 HTTP 배포를 막는 설정 검사가 없었음 | Host 허용 목록, Origin/Fetch Metadata 검사. 외부 바인딩/production에는 HTTPS PUBLIC_ORIGIN 요구 |

HTTPS URL 설정과 HSTS 헤더만으로 TLS 연결이 만들어지지는 않습니다. 실제 HTTPS 리버스 프록시와 인증서, HTTP→HTTPS 전환, 백엔드 포트 외부 차단을 배포 환경에서 적용해야 합니다. 프록시는 원래 공개 Host를 전달해야 합니다. CSP의 style-src unsafe-inline은 현재 동적 스타일과 인쇄 CSS 때문에 유지했으며 script-src에는 허용하지 않았습니다.

## 남아 있는 문제 — 배포 전 확인

1. **높음 / 운영 조건부: 서버 재시작·다중 인스턴스의 비용 통제 한계**
   `server.mjs:21,68,72`: 발송 제한과 중복 ID는 프로세스 메모리에만 있습니다. 재시작하면 사라지고 여러 서버에서는 공유되지 않습니다. 동일 사진도 새 ID/새 세션으로 재요청하면 별도 발송됩니다. 인증된 기기가 악용되거나 장애 후 다시 요청할 때 중복/과금 위험이 남습니다. 공유 저장소의 원자적 발송 제한/중복 처리, 수신번호별 제한 및 공급자 비용 알림을 적용해야 합니다. 지금의 ID 중복 처리는 동일 요청 재전송을 막는 수준이며 exactly-once 발송 보장이 아닙니다.

2. **중간: 서버 이미지 검증이 JPEG 헤더 수준**
   `server.mjs:8,71`: 서명·일부 SOF 크기는 검사하지만 전체 JPEG를 디코딩하지 않습니다. 테스트용 23바이트짜리 SOF 헤더도 공급자 호출 단계까지 도달함을 확인했습니다. 인증된 요청자가 손상 파일/불필요한 메타데이터를 전달할 수 있습니다. 브라우저 정상 경로에서는 canvas로 다시 인코딩하지만 API 직접 호출자는 우회할 수 있습니다. 서버에서 제한된 자원으로 디코딩·재인코딩하고 메타데이터를 제거하는 검증이 필요합니다. 로컬 코드 실행 취약성이 입증된 것은 아닙니다.

3. **중간: 전역 로그인 제한으로 정상 운영 방해 가능**
   `server.mjs:53`: 익명 사용자가 로그인 요청 10회를 먼저 사용하면 신규 활성화가 최대 10분 제한됩니다. 이미 활성화된 세션의 발송은 영향을 받지 않습니다. 신뢰하는 프록시를 명시한 접속자별 제한 또는 운영자 활성화 경로의 접근 제한이 필요합니다. 현재는 X-Forwarded-For를 신뢰하지 않습니다.

4. **중간: 공용 iPad 이용자 간 사진 노출**
   `dist/app.js:237`: 초기화 버튼을 누르기 전에는 사진이 화면/메모리에 남습니다. 자동 유휴 초기화, 운영자의 다음 이용자 전 확인 절차가 아직 없습니다. 저장한 PNG/JPG/HTML과 시스템 인쇄 파일은 앱 초기화로 지워지지 않습니다. iPad 다운로드·사진 앱 정리, 행사 전용 기기 모드가 필요합니다. JS 참조 제거는 기기 메모리의 보안 삭제를 보장하지 않습니다.

5. **중간 / 운영 조건부: 활성화된 기기의 권한 범위**
   `dist/delivery.js:2`, `server.mjs:64`: 운영자 활성화 후 12시간 동안 허용된 요청을 보낼 수 있으며 별도 서버 세션 폐기 UI가 없습니다. 번호 소유 확인(OTP), 실제 촬영한 결과에만 발급하는 발송 권한, 영구 동의 증적은 없습니다. 동의 true는 요청값 검사이며 번호 소유자 본인의 동의라는 증명은 아닙니다. 행사장 기기 통제와 운영자 감독이 전제입니다.

## 확인한 기존 방어

- SENS 비밀 키는 서버 환경 변수로만 읽고 브라우저에 제공하지 않습니다. 저장소에서 실제 `.env`는 발견되지 않았습니다.
- 프런트엔드 텍스트는 textContent를 사용하며, 검토한 JS에는 innerHTML/eval이 없습니다. SQL/셸 명령을 사용하는 발송 경로도 없습니다. 이는 모든 취약점 부재의 증명은 아닙니다.
- 사진과 번호를 앱 DB·파일에 저장하는 코드는 없고, 카메라는 오디오를 요청하지 않습니다. SENS에는 동의 후 사진/번호가 전달되므로 서비스 측 보관은 별도입니다.
- 인증 토큰은 난수 32바이트이며 브라우저 localStorage/cookie에 저장하지 않습니다. API 응답은 no-store입니다.
- 수신번호, 동의 여부와 정책 버전을 서버에서 검사하고 메시지 본문/발신번호/공급자 URL은 서버가 고정합니다.
- package.json에 외부 런타임 npm 의존성은 없습니다. npm audit 결과를 보안 보증으로 제시하지 않으며 Node/브라우저 및 OS의 최신 보안 패치는 별도입니다.

## 검증

`npm run check`와 `npm test` 통과. 촬영 10장/4장 선택/필터/인쇄 회귀, 인증·동의·중복 처리, 설정 오류 차단, 경로 순회/비밀 파일/심볼릭 링크 차단, Host/Origin 검사, 본문 제한, 잘못된 타입, 공급자 실패 재시도 억제 및 로그인 제한을 검사했습니다. 실제 공격자가 장시간 요청하는 부하 테스트·외부 침투 테스트는 수행하지 않았습니다.

기준 참고: [OWASP REST Security](https://cheatsheetseries.owasp.org/cheatsheets/REST_Security_Cheat_Sheet.html), [OWASP File Upload](https://cheatsheetseries.owasp.org/cheatsheets/File_Upload_Cheat_Sheet.html), [OWASP HTTP Headers](https://cheatsheetseries.owasp.org/cheatsheets/HTTP_Headers_Cheat_Sheet.html).
