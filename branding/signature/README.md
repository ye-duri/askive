# Yonsei Studio signature
Selected design D: handwritten Yonsei with STUDIO subtitle. The site pairs it with a separate accessible Korean name; print frames use English only.
Generated with the built-in image generator from the approved reference. Production prompt: Extract only the exact Yonsei signature and spaced STUDIO subtitle, black ink, genuine transparent background, preserve stroke shape, no Korean or decorations.
`yonsei-signature.png` is the high-resolution transparent source. `scripts/build-basic-collection.py` embeds it into self-contained SVGs and recolors the alpha mask. Logo width is 324px / 1200px (27%); no font installation or external image request is needed.
Frames: 1200×1776 pixels, P postcard 100×148mm ratio. 9 colors × 3 layouts × 3 cut counts = 81. See docs/basic-frame-layouts.json for exact photo coordinates. Special edition frames are preserved.
